# Linear Integration

**⚠️ MANDATORY: Read and expand the import below FIRST, then follow ALL instructions from the base file.**


When referred to sub-agents like codebase-analyzer, codebase-locator, codebase-pattern-finder, web-search-researcher, thoughts-analyzer or thoughts-locator treat those commands agentically and wait for their response before continuing (they will become parallel later right now you as Cursor does not have subagents so this is a workaround)

#import ../../.agents/commands/linear.base.md

ARGUMENTS: [Replace this with Linear command, e.g., "update FSC-299" or "create issue"]


